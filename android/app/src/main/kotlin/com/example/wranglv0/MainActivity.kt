package com.example.wranglv0

import io.flutter.embedding.android.FlutterActivity

// App-launch and screen-capture channels now live in the `wrangl_native`
// plugin, which auto-registers on every engine (launcher + overlay isolate).
class MainActivity : FlutterActivity()
